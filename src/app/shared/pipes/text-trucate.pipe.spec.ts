import { TextTrucatePipe } from './text-trucate.pipe';

describe('TextTrucatePipe', () => {
  it('create an instance', () => {
    const pipe = new TextTrucatePipe();
    expect(pipe).toBeTruthy();
  });
});
